

<ul>

	<% @projects.each do |one_project| %>
	
	<li> <%= link_to one_project.name, project_path(one_project) %> 


	<% if one_project.description == nil %>
	
	<% one_project.name %>

	<% else %>

	<% one_project.name %> <% one_project.description %></li>

	<% end %>
<% end %>
</ul>

<%= link_to "Create a new project", projects_new_path %>